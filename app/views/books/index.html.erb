<p>Welcome! You have signed up successfully.</p>

<main>
  <div>
    <%= form_with model: @user, url: edit_user_registration_path, method: :get do |f| %>

      <h2>User info</h2>
      <%= image_tag 'no_image', size: "100x100" %>
      <tr>
        <th>name</th>
        <th><%=@user.name %></th>
      </tr>
      <tr>
        <th>introduction</th>
        <th><%=@user.introduction %></th>
      </tr>
    
    <li>
      <%= link_to "アイコン", edit_user_path(@user.id) %>
    </li>     
  
    <% end %>
  </div>

<!-- form_with部分 -->

  <%= form_with model: @book, url: books_path, method: :post do |f| %>

    <h2>New book</h2>

    <h5>Title</h5>
    <%= f.text_field :title %>
	
    <h5>Body</h5>
    <%= f.text_area :Opinion %>
 
    <%= f.submit 'Create Book' %>
  <% end %>

  <% if @book.errors.any? %>
    <%= @book.errors.count %>errors prohibited this book from being saved:
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>
</main>



<h2>Books</h2>

<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
  </tr>
  <record>
    <% @books.each do |book| %>
      <tr>
        <th><%= book.title %></th>
        <th><%= book.body %></th>
        <th><%= link_to "Show", book_path(book.id) %></th>
        <th><%= link_to "Edit", edit_book_path(book.id) %></th>
        <th><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure?"%></th>
      </tr>
    <% end %>
  </record>


</table>  
<!-- form_with部分 -->
<%= form_with model: @book, url: books_path, method: :post do |f| %>

  <h3>New book</h3>

  <h5>Title</h5>
  <%= f.text_field :title %>
	
  <h5>Body</h5>
  <%= f.text_area :body %>
 
  <%= f.submit 'Create Book' %>
<% end %>

<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>