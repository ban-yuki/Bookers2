<p>Welcome! You have signed up successfully.</p>

<%= render 'users/list', user: @user,book: @newbook %>

<table class="table table-striped">
<h2>Books</h2>

  <thead>
  <tr>
    <th>Title</th>
    <th>Opinion</th>
  </tr>
  </thead>
  <tbody>
  <tr>
    <th><%= link_to @book.title, books_path(@book.id) %></th>
    <th><%= @book.body %></th>
  </tr>
  </tbody>

</table>
<!-- form_with部分 -->
